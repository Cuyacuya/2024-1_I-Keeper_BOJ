배열을 돌면서 i번째와 i+1번째 값이 다르면 i번째 값의 수를 count로 +1, 최종적으로 0의 카운트와 1의 카운트를 비교해서 출력
라고 최초에 생각했는데 생각보다 잘 모르겠어서 검색을 해보았고, 모르는 것들 투성이라 다시 생각해봐야할 듯...

Q1. strdup()함수를 쓰는 이유? 정작 사용은 str[i]로 하면서 strdup()함수를 쓰면서까지 사용하는 이유 알아보기
Q2. 그냥 str[i]로 두면 str[i]에 값이 있으면이 맞는지?
