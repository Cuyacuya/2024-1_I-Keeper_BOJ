The logic of what I thought : 2693

크기가 10인 int배열 A를 만든다.

테스트케이스 T를 만들어 몇 개의 배열을 받을지 입력받는다.

반복문과 입력을 통해 T개수만큼의 배열을 입력받는다.

버블 정렬로 오름차순 정렬을 만든다.

N번째 큰 수를 출력으로 받는데 이때 N은 3번째가 고정이므로 3번째로 큰 수를 출력한다.

오름차순으로 정렬한 경우 3번째로 큰 수는 배열의 인덱스상 7번이므로 인덱스 7번을 출력한다.

 

=> 근데 생각해보니 C는 동적할당을 해야하는데 뭔가 쓰기가 싫다. 다른걸로 다시 생각해보자...

(C 공부할 시간에 C#을 공부해서 이 문제를 풀어보고싶다.)

 

해결책은 T만큼 계속 배열을 받을 수 있게 하면 된다. 

 

반복문을 통해서 T만큼 계속 돌리고

반복문안에서 배열의 값을 10개씩 입력받고 

버블정렬로 오름차순으로 정렬해서 

3번째로 큰 수가 저장된 인덱스 7을 출력한다.

 

이때 반복문은 for를 쓰면 버블정렬을 쓸 때에도 이중 포문을 쓰니 전체코드 3중 포문이 되므로 while을 써보자

 

테스트 케이스 T를 받아서 while()문을 통해 T가 0이 될 때까지. 이런식으로 하면 좋을 것 같다.